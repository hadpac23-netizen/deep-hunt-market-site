const assert=require("node:assert");const path=require("node:path");const {pathToFileURL}=require("node:url");
(async()=>{const {evaluateDeploymentReadiness:e}=await import(pathToFileURL(path.resolve("deployment-readiness-core.mjs")));
const source={migration_source:true,runway_source:true,vault_source:true,vision_source:true,tests_passed:true};
const current=e({...source,ledger_table_exists:false,ledger_rls_enabled:false,vault_bucket_exists:false,vault_bucket_private:false,ledger_policy_exists:false,vault_policy_exists:false,runway_function_deployed:false,vault_function_deployed:false,runway_verify_jwt:false,vault_verify_jwt:false,runway_secret_present:null,vision_secret_present:null,runway_enabled:false,vision_enabled:false,owner_activation_granted:false,product_truth_live_verified:false});
assert.equal(current.status,"RUNTIME_BLOCKED");assert.equal(current.source_ready,true);assert.equal(current.runtime_ready,false);assert.equal(current.generation_ready,false);assert.equal(current.provider_calls_made,0);
const ready=e({...source,ledger_table_exists:true,ledger_rls_enabled:true,vault_bucket_exists:true,vault_bucket_private:true,ledger_policy_exists:true,vault_policy_exists:true,runway_function_deployed:true,vault_function_deployed:true,runway_verify_jwt:true,vault_verify_jwt:true,runway_secret_present:true,vision_secret_present:true,runway_enabled:true,vision_enabled:true,owner_activation_granted:true,product_truth_live_verified:true});
assert.equal(ready.status,"READY_FOR_FIRST_GENERATION");assert.equal(ready.generation_ready,true);assert.equal(ready.publishing_authorized,false);console.log("deployment_readiness_tests=PASS");})().catch(e=>{console.error(e);process.exit(1)});
